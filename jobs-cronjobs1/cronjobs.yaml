apiVersion: batch/v1
kind: CronJob
metadata:
  name: hello
spec:
  schedule: "* * * * *"
  jobTemplate:
    spec:
      failedJobsHistoryLimit: 2
      backoffLimit: 1
      template:
        spec:
          containers:
            - name: hello
              image: alpine
              imagePullPolicy: IfNotPresent
              command:
                - "/bin/sh"
                - "-c"
                - "sleep 4 & exit 1"
          restartPolicy: Never

