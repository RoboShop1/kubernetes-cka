apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: deny-all
  namespace: sample
spec:
  selector:
    matchLabels:
      app: my-app
  action: DENY
  rules:
    - to:
        - operation:
            methods: ["GET","DELETE"]